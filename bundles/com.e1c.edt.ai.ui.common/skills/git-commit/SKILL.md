---
name: git-commit
description: Сгенерируй сообщение для коммита приведённых ниже изменений. Используй стиль сообщений по примеру из последних коммитов. Если их нет, используй стандартизированный стиль коммит-сообщений с кратким описанием изменений
---

Сгенерируй сообщение для коммита на основе изменений.

Текущий diff:
!tool('git_diff')							

Последние commit messages:
!tool('git_recent_commits')					

Дополнительный ввод пользователя:
${user_text}							

Верни только текст commit message.