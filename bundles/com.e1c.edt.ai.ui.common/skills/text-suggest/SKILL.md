---
name: text-suggest
description: Предложить свой вариант текста для поля, находящегося в фокусе, на основе окружающего визуального контекста.
---

Предложи свой вариант текста для поля ввода, с которым сейчас работает пользователь.
Поле может быть пустым — в этом случае предложи содержимое с нуля, опираясь на соседние поля
и назначение диалога из визуального контекста.

## Данные редактируемого поля

- Имя поля: ${field_name}
- Многострочное поле (is_multiline): ${is_multiline}
- Текущее значение поля (может быть пустым):
```
${field_value}
```
- Выделенный пользователем текст (если пусто — работай со значением целиком):
```
${selected_text}
```

## Визуальный контекст среды разработки

Ниже приведён JSON-снимок того, что пользователь сейчас видит в IDE: активное окно и открытые диалоги
(заголовки, поля и их значения, флажки, списки, таблицы с видимыми строками, вкладки), признак фокуса
("is_focused": true), активный редактор ("active_editor" — видимая область, выделение, позиция курсора)
и буфер обмена ("clipboard"). Используй его, чтобы понять предметную область и назначение редактируемого поля.

```json
!tool('visual_context')
```

## Требования к обработке

- Учитывать предметную область (заголовок окна и соседние поля из визуального контекста)
- Проанализировать все поля визуального контекста
- Предложить свой вариант текста для редактируемого поля
- Исключить из результата дополнительные символы, разметку, пояснения, рассуждения
- Не обрамлять ответ символами " ' ` [ ] < >
- Если is_multiline = false, результат должен быть одной строкой
- Если is_multiline = true, результат должен содержать более 10 слов
- Для поля с именем "Имя" результат должен содержать только буквы и цифры
- Для поля с именем "Комментарий" результат должен содержать подробный комментарий
- Для поля с именем "Пояснение" результат должен содержать подробное пояснение
- Язык ответа: ${language} (если соседние поля явно на другом языке — используй язык этих полей)

## Алгоритм работы

- Определить язык по соседним полям
- Классифицировать поле по имени "${field_name}" и по визуальному контексту
- Предложить текст для значения поля с учётом остальных полей
- Вернуть предложенный текст

## Примеры

- Для пустого поля "Пояснение" в диалоге обработки «ОбработкаВозврата» (синоним «Обработка возврата
  неисправного товара») ожидаемый результат: Выполняет обработку возврата неисправных товаров
- Для пустого поля "Имя" при синониме «Обработка возврата неисправного товара» ожидаемый результат: ОбработкаВозврата

Верни только итоговый текст поля.
