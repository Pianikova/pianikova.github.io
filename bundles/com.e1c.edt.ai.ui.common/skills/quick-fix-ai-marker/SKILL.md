---
name: quick-fix-ai-marker
description: Применяет исправление, предложенное AI-маркером 1С:Напарника (действие из фонового анализа кода). Используется быстрым исправлением по маркеру из редактора и Problems view.
---

# Применение исправления AI-маркера

Примени исправление, предложенное AI-маркером 1С:Напарника, и подтверди результат повторной проверкой маркеров.

## Входные данные

- Файл: `${absolute_file_path}`
- Строка: `${line}`
- Идентификатор маркера: `${marker_id}`
- Другие проблемы на той же строке (исправь их заодно, если уместно): ${additional_problems}
- Детали маркера:

```
${marker_details}
```

Предложенное исправление:

${action_prompt}

## Порядок работы

1. Прочитай инструментом `Read` текущее содержимое файла `${absolute_file_path}` вокруг строки `${line}` (±20 строк) — содержимое могло измениться с момента создания маркера.
2. Примени исправление инструментом `Edit`. Не изменяй несвязанный код, не переформатируй файл.
3. Перепроверь ai-маркеры файла: удали решённые (включая маркер с id `${marker_id}`), добавь ai-маркеры на новые проблемы, если правка их породила.
4. **Обязательно проверь результат**: вызови инструмент `GetMarkers` для файла `${absolute_file_path}` и убедись, что исправленная проблема исчезла и правка не породила новых ошибок. Если проблема осталась или появились новые — исправь их и проверь `GetMarkers` ещё раз (не более 3 попыток).

## Формат вывода

Кратко (1–2 предложения) опиши, что изменено и почему, и подтверди результат проверки маркеров.
